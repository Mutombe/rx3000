The logo now appears on the sign-in card, and the till application can
update itself.

- Every list that writes through a dialog shows the row straight away
  rather than after the server answers.
- The navigation on a phone is a rail of icons with the menu button in
  the top bar.
- Signing in and every page load are several times faster.
- A new version is offered in the top bar. Nothing installs until you
  say so.
