fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
anthropic>=0.40
apscheduler>=3.10
PyJWT>=2.8
python-multipart>=0.0.9
# Postgres driver for hosted deployments. SQLite stays the default for a
# pharmacy running on its own till; a shared server needs a real database.
# Not pinned to an exact version: the build host runs Python 3.14, and 3.2.3
# has no wheel for it — pinning it meant pip had nothing to install and the
# build died. A floor, not a pin.
psycopg[binary]>=3.2.10

# The dispensing shorthand code book prints to PDF for MCAZ inspection. Pure
# Python, no system libraries, which is why this rather than WeasyPrint: a
# pharmacy server should not need GTK installed to produce a one-page sheet.
reportlab>=4.0

# EVERY WORKBOOK THIS PRODUCT PRODUCES, AND IT WAS NEVER DECLARED.
#
# `services/reports/engine.py` writes real xlsx, `services/spreadsheet.py`
# reads one, and the export endpoint writes one for every grid. All three
# import openpyxl, none of them declared it, and it was installed on the
# machine it was written on — so the tests passed, the guards passed, and on
# the hosted deployment every one of those downloads answered 500 while CSV
# answered 200. Both routes default to xlsx, so the commonest press was the
# broken one.
#
# It had been failing since reports were written. Nobody reported it because a
# 500 from a download link looks like the internet, not like the software, and
# because the developer's laptop has openpyxl and always will.
openpyxl>=3.1

# Off-machine backups to an S3-compatible bucket (DigitalOcean Spaces here;
# Backblaze, MinIO and AWS all speak the same protocol). Optional at runtime:
# with no credentials set, services/objects.py is inert and a pharmacy running
# one machine in a back office never calls it. Installed anyway because the
# hosted deployment is where the local filesystem is ephemeral and a backup
# written to it is thrown away by the next deploy.
boto3>=1.34
